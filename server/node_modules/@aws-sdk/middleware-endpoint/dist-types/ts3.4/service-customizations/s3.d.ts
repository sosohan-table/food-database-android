import {
  EndpointParameters,
  HandlerExecutionContext,
  SerializeHandlerArguments,
} from "@aws-sdk/types";
import { PreviouslyResolvedServiceId } from "../endpointMiddleware";
import { EndpointResolvedConfig } from "../resolveEndpointConfig";
import { EndpointParameterInstructions } from "../types";
export declare const s3Customizations: <T extends EndpointParameters>(
  config: EndpointResolvedConfig<T> & PreviouslyResolvedServiceId,
  instructions: EndpointParameterInstructions,
  args: SerializeHandlerArguments<any>,
  context: HandlerExecutionContext
) => Promise<void>;
export declare const DOT_PATTERN: RegExp;
export declare const S3_HOSTNAME_PATTERN: RegExp;
export declare const isDnsCompatibleBucketName: (bucketName: string) => boolean;
