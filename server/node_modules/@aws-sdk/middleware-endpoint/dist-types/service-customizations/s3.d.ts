import { EndpointParameters, HandlerExecutionContext, SerializeHandlerArguments } from "@aws-sdk/types";
import { PreviouslyResolvedServiceId } from "../endpointMiddleware";
import { EndpointResolvedConfig } from "../resolveEndpointConfig";
import { EndpointParameterInstructions } from "../types";
/**
 * Bucket name DNS compatibility customization.
 */
export declare const s3Customizations: <T extends EndpointParameters>(config: EndpointResolvedConfig<T> & PreviouslyResolvedServiceId, instructions: EndpointParameterInstructions, args: SerializeHandlerArguments<any>, context: HandlerExecutionContext) => Promise<void>;
export declare const DOT_PATTERN: RegExp;
export declare const S3_HOSTNAME_PATTERN: RegExp;
/**
 * Determines whether a given string is DNS compliant per the rules outlined by
 * S3. Length, capitaization, and leading dot restrictions are enforced by the
 * DOMAIN_PATTERN regular expression.
 * @internal
 *
 * @see https://docs.aws.amazon.com/AmazonS3/latest/dev/BucketRestrictions.html
 */
export declare const isDnsCompatibleBucketName: (bucketName: string) => boolean;
